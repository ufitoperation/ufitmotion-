Flask==3.1.3
Flask-Limiter==3.9.0
gunicorn==23.0.0
httpx==0.28.1
psycopg[binary]==3.3.3
python-dotenv==1.2.1
sentry-sdk[flask]==2.28.0
Werkzeug==3.1.6

# Dev / test only — not needed in production
# Move these to requirements-dev.txt if you want a leaner production image
pytest==8.3.5
